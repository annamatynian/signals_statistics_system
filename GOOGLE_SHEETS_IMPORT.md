# 📥 Импорт сигналов из Google Sheets

Система поддерживает импорт сигналов напрямую из Google Sheets, минуя Gradio UI.

## 📋 Формат таблицы Google Sheets

### Требуемые колонки

Создайте таблицу с следующими колонками (первая строка = заголовки):

| name | channel_name | symbol | take_profit | stop_loss | exchange | target_price | condition | active |
|------|--------------|--------|-------------|-----------|----------|--------------|-----------|--------|
| BTC Long #1 | VIP Crypto Signals | BTCUSDT | 100000 | 85000 | binance | 90000 | above | TRUE |
| ETH Trade | Budget Trading | ETHUSDT | 3800 | 3200 | binance | 3500 | above | TRUE |
| BNB Scalp | Pro Traders Hub | BNBUSDT | 650 | 580 | binance | 620 | above | TRUE |

### Описание колонок

#### ✅ Обязательные колонки

| Колонка | Описание | Пример |
|---------|----------|--------|
| **channel_name** | Название телеграм канала (ОБЯЗАТЕЛЬНО!) | `VIP Crypto Signals` |
| **symbol** | Торговая пара | `BTCUSDT` |
| **take_profit** | Цена Take Profit (TP) | `100000` |
| **stop_loss** | Цена Stop Loss (SL) | `85000` |

#### ⚙️ Опциональные колонки

| Колонка | Описание | По умолчанию |
|---------|----------|--------------|
| **name** | Название сигнала | Auto: `{symbol} - {channel_name}` |
| **exchange** | Биржа (`binance`, `bybit`, `coinbase`) | `binance` |
| **target_price** | Целевая цена входа | Используется `take_profit` |
| **condition** | Условие (`above`, `below`, `equal`) | `above` |
| **active** | Активен ли сигнал (`TRUE`/`FALSE`) | `TRUE` |

### ⚠️ Важные правила

1. **TP должен быть больше SL** (take_profit > stop_loss)
2. **Все цены должны быть числами** (не используйте $ или запятые)
3. **Symbol должен быть заглавными** (BTCUSDT, не btcusdt)
4. **Channel_name обязателен** - система привязывает сигналы к каналам
5. **Первая строка = заголовки** колонок

---

## 🔧 Настройка Google Sheets API

### Шаг 1: Создать Service Account

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Sheets API**
4. Создайте **Service Account**:
   - IAM & Admin → Service Accounts → Create Service Account
   - Скачайте JSON ключ

### Шаг 2: Настроить доступ к таблице

1. Откройте вашу Google Sheets таблицу
2. Нажмите "Share"
3. Добавьте email из Service Account (из JSON файла, поле `client_email`)
4. Дайте права "Viewer" (только чтение)

### Шаг 3: Скопировать ID таблицы

Из URL таблицы скопируйте ID:
```
https://docs.google.com/spreadsheets/d/SPREADSHEET_ID_HERE/edit
                                         ^^^^^^^^^^^^^^^^
```

### Шаг 4: Установить переменные окружения

```bash
# Содержимое JSON файла Service Account (одной строкой)
export GOOGLE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"...",...}'

# ID вашей таблицы
export GOOGLE_SHEETS_SPREADSHEET_ID='1abc...xyz'
```

Или создайте файл `.env`:
```env
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
GOOGLE_SHEETS_SPREADSHEET_ID=1abc...xyz
```

---

## 🚀 Использование

### Простой импорт

```bash
# Импорт из листа "Signals" (по умолчанию)
python3 import_from_sheets.py

# Импорт из другого листа
python3 import_from_sheets.py "My Signals"
```

### Что происходит при импорте

1. ✅ **Подключение к Google Sheets**
2. ✅ **Чтение сигналов** из указанного листа
3. ✅ **Валидация данных** (проверка обязательных полей)
4. ✅ **Автосоздание каналов** (если их нет в базе)
5. ✅ **Импорт сигналов** в базу данных
6. ✅ **Обновление статистики**

### Пример вывода

```
============================================================
📥 Google Sheets Signal Importer
============================================================

✅ Storage initialized: data/signals.json

🔗 Testing Google Sheets connection...
✅ Connection successful!

📊 Reading from sheet: 'Signals'

👀 Preview of signals in sheet:
Found 10 signals:
  - BTC Long #1 (BTCUSDT) - VIP Crypto Signals
  - ETH Trade (ETHUSDT) - Budget Trading
  - BNB Scalp (BNBUSDT) - Pro Traders Hub
  ... and 7 more

📥 Import these signals? (yes/no): yes

📥 Importing signals...

============================================================
📊 IMPORT RESULTS
============================================================
Total rows processed: 10
Successfully imported: 10
Skipped: 0

Channels auto-created: 2
  - VIP Crypto Signals
  - Budget Trading

✅ Import completed successfully!

📊 Updating statistics...
✅ Statistics updated!

📈 Current system state:
  Total channels: 3
  Total signals: 44
  Active signals: 14

🚀 Done! You can now:
  1. Run 'python3 run_simple.py' to view in UI
  2. Check data/signals.json for imported data
```

---

## 📊 Пример таблицы

### Минимальный пример (только обязательные поля)

| channel_name | symbol | take_profit | stop_loss |
|--------------|--------|-------------|-----------|
| VIP Crypto Signals | BTCUSDT | 100000 | 85000 |
| VIP Crypto Signals | ETHUSDT | 3800 | 3200 |
| Budget Trading | BNBUSDT | 650 | 580 |

### Полный пример (все поля)

| name | channel_name | symbol | exchange | target_price | take_profit | stop_loss | condition | active |
|------|--------------|--------|----------|--------------|-------------|-----------|-----------|--------|
| BTC Moon Shot | VIP Crypto Signals | BTCUSDT | binance | 90000 | 100000 | 85000 | above | TRUE |
| ETH Breakout | VIP Crypto Signals | ETHUSDT | binance | 3500 | 3800 | 3200 | above | TRUE |
| BNB Quick | Budget Trading | BNBUSDT | binance | 620 | 650 | 580 | above | TRUE |
| SOL Long | Pro Traders Hub | SOLUSDT | binance | 200 | 250 | 180 | above | TRUE |
| AVAX Trade | Pro Traders Hub | AVAXUSDT | binance | 100 | 120 | 85 | above | FALSE |

---

## 🔄 Workflow

### Вариант 1: Регулярный импорт

```bash
# 1. Обновите таблицу в Google Sheets
# 2. Запустите импорт
python3 import_from_sheets.py

# 3. Просмотрите результаты в UI
python3 run_simple.py
```

### Вариант 2: Автоматический импорт (cron)

Добавьте в crontab для автоматического импорта каждый час:

```bash
crontab -e

# Добавьте строку:
0 * * * * cd /path/to/signals_statistics_system && python3 import_from_sheets.py >> import.log 2>&1
```

---

## ❓ FAQ

### Q: Что если канал уже существует?
**A:** Система использует существующий канал. Новый НЕ создается.

### Q: Что если сигнал дублируется?
**A:** Будет создан новый сигнал с новым ID. Дубликаты разрешены.

### Q: Можно ли импортировать в существующую базу?
**A:** Да! Импорт добавляет сигналы к существующим, не удаляя старые.

### Q: Как обновить существующий сигнал?
**A:** Импорт не обновляет сигналы, только добавляет новые. Для обновления используйте Gradio UI.

### Q: Что делать если импорт падает с ошибкой?
**A:** Проверьте:
1. Валидность JSON credentials
2. Права доступа Service Account к таблице
3. Формат данных в таблице (все обязательные колонки заполнены)
4. TP > SL для всех сигналов

---

## 🎯 Преимущества Google Sheets импорта

✅ **Быстрый массовый импорт** - добавьте 100+ сигналов одной командой
✅ **Совместная работа** - команда может редактировать таблицу
✅ **История изменений** - Google Sheets хранит версии
✅ **Excel-like интерфейс** - привычный формат таблиц
✅ **Автосоздание каналов** - не нужно создавать каналы вручную
✅ **Валидация данных** - система проверит корректность перед импортом

---

## 📚 Связанные файлы

- `src/services/sheets_importer.py` - Класс импорта
- `import_from_sheets.py` - Скрипт импорта
- `.env.example` - Пример конфигурации

---

**Готово к использованию!** 🚀

Просто создайте таблицу в Google Sheets, настройте доступ и запустите `python3 import_from_sheets.py`.
