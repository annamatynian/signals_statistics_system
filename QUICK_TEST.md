# ⚡ Быстрое тестирование

## 🚀 Вариант 1: Пустая система (для ручного тестирования)

```bash
# 1. Очистить данные
rm -f data/signals.json

# 2. Запустить систему
python3 run.py

# 3. Открыть в браузере
# http://localhost:7860

# 4. Добавить сигналы вручную через веб-форму
```

---

## 📊 Вариант 2: С демо-данными (для быстрого просмотра)

```bash
# 1. Очистить данные
rm -f data/signals.json

# 2. Добавить демо-данные
python3 add_demo_data.py

# Результат:
# ✅ 3 канала
# ✅ 4 активных сигнала
# ✅ 30 закрытых сигналов с результатами
# ✅ Статистика рассчитана

# 3. Запустить систему
python3 run.py

# 4. Открыть в браузере
# http://localhost:7860
```

**Ожидаемые данные:**
- VIP Crypto Signals: 70% winrate (7W/3L)
- Budget Trading Channel: 40% winrate (4W/6L)
- Pro Traders Hub: 80% winrate (8W/2L)

---

## 🧪 Вариант 3: Автоматический тест (для проверки работы)

```bash
# 1. Запустить интеграционный тест
python3 test_integration.py

# Результат:
# ✅ ALL TESTS PASSED!
# ✅ Винрейт 70%, 40%, 55% проверен
```

---

## 📝 Быстрая проверка функций

### Проверка 1: Добавление сигнала
1. Открыть http://localhost:7860
2. Вкладка "➕ Add Signal"
3. Заполнить:
   - Signal Name: Test BTC
   - Channel Name: Test Channel
   - Symbol: BTCUSDT
   - TP: 100000
   - SL: 80000
4. Нажать "Add Signal"
5. ✅ Должно показать успех

### Проверка 2: Просмотр сигналов
1. Вкладка "📋 Active Signals"
2. ✅ Должен отобразиться добавленный сигнал

### Проверка 3: Статистика
1. Вкладка "📊 Channel Statistics"
2. ✅ Должен отобразиться канал "Test Channel"
3. ✅ Total Signals: 1

---

## 🛠 Полезные команды

### Очистка данных
```bash
rm -f data/signals.json
```

### Просмотр данных
```bash
cat data/signals.json | python3 -m json.tool
```

### Проверка структуры
```bash
python3 -c "
import json
with open('data/signals.json') as f:
    data = json.load(f)
    print(f'Signals: {len(data.get(\"signals\", []))}')
    print(f'Channels: {len(data.get(\"channels\", []))}')
    print(f'Statistics: {len(data.get(\"statistics\", {}))}')
"
```

### Остановка сервера
```bash
# Ctrl+C в терминале где запущен run.py
```

---

## 📋 Чеклист минимального теста (5 минут)

- [ ] `python3 add_demo_data.py` - добавить данные
- [ ] `python3 run.py` - запустить систему
- [ ] Открыть http://localhost:7860
- [ ] Вкладка "Active Signals" - видны сигналы
- [ ] Вкладка "Channel Statistics" - видна статистика
- [ ] Добавить новый сигнал через форму
- [ ] Обновить статистику кнопкой Refresh
- [ ] ✅ Все работает!

---

## 🐛 Если что-то не работает

### Ошибка: ModuleNotFoundError
```bash
pip install pydantic gradio pandas requests python-dotenv
```

### Ошибка: Port already in use
```bash
# Убить процесс на порту 7860
lsof -i :7860
kill -9 <PID>
```

### Ошибка: Permission denied
```bash
chmod +x run.py
chmod +x add_demo_data.py
chmod +x test_integration.py
```

---

## ⏱️ Время тестирования

| Вариант | Время | Назначение |
|---------|-------|------------|
| Вариант 1 | 10 мин | Ручное тестирование UI |
| Вариант 2 | 2 мин | Быстрый просмотр возможностей |
| Вариант 3 | 1 мин | Автоматическая проверка |

**Рекомендация:** Начните с Варианта 2 для быстрой демонстрации!
